
<div class="flex flex-col gap-5 mt-5 mx-2  lg:col-span-1 col-span-full animate-pulse text-transparent">
    <div class="flex flex-col gap-5 w-full md:w-fit h-fit rounded-xl">
        <div class="flex flex-row gap-2">
          <span class="text-lg font-bold h-6 w-full  bg-skeleton animate-pulse rounded-sm">Loading alerts </span>
        </div>
        <span class="text-lg font-bold w-12.5 h-6.25 bg-skeleton animate-pulse rounded-full"></span>
        <span class="text-lg font-bold w-full h-4 bg-skeleton animate-pulse rounded-sm"></span>
    </div>
    <div class="flex flex-col gap-5">
        
      <div class="hidden flex-row gap-2 mt-2 mb-2 md:flex">
        <div class="h-4 w-4 bg-skeleton rounded-sm" />
        <div class="h-4 w-20 bg-skeleton rounded-sm" />
      </div>

      <div class="h-11 w-34 bg-skeleton rounded-full flex md:hidden"></div>

      {/* Buscar */}
      <div class="hidden flex-col gap-2 md:flex">
        <div class="h-4 w-20 bg-skeleton rounded-sm" />
        <div class="h-8 w-full bg-skeleton rounded-sm" />
      </div>

        {/* Especialidad */}
      <div class="hidden flex-col gap-2 md:flex">
        <div class="h-5 w-32 bg-skeleton mb-1 rounded-sm" />
        <div class="flex flex-col gap-1">
          {Array.from({ length: 8 }).map((_, i) => (
            <div class="flex flex-row items-center gap-2 mb-2">
              <div class="h-6 w-6 bg-skeleton rounded-sm" />
              <div class="h-4 w-32 bg-skeleton rounded-sm" />
            </div>
          ))}
        </div>
      </div>

      {/* Fecha */}
      <div class="hidden flex-col gap-2 md:flex">
        <div class="h-4 w-20 bg-skeleton rounded-sm" />
        <div class="h-8 w-full bg-skeleton rounded-sm" />
      </div>

        {/* Tiempo */}
      <div class="hidden flex-col gap-2 md:flex">
        <div class="h-4 w-20 bg-skeleton rounded-sm" />
        <div class="h-8 w-full bg-skeleton rounded-sm" />
      </div>

      {/* Botón */}
      <div class="hidden md:flex">
        <div class="h-10 w-full bg-skeleton rounded-full mt-2" />
      </div>
    </div>
  </div>